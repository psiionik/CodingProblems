#ifndef SORTEDDIFF_H
#define SORTEDDIFF_H

#include <vector>

using namespace std;

class SortedDiff {
    public: 
        void getSorted(int arr_len, vector<int> arr);
        void getSortedAnswer(int arr_len, vector<int> arr);
};

#endif